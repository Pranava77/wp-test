/* ==========================================================================
   WP Learn Kit — front-end script.
   The original demo banner (Subsystem 4) was removed, so there is nothing for
   this script to do on the front end anymore. Left intentionally empty (rather
   than deleted) so the enqueue handle in Subsystem 8 still resolves.
   ========================================================================== */
